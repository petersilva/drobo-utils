# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall droboutils
	if grep -qs '^droboutils$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^droboutils$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
